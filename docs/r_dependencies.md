# R dependencies

The diet optimizer requires R 4.1 or newer, `ompr`, `ompr.roi`, `ROI`, and `ROI.plugin.glpk`, with a working GLPK backend.

```r
install.packages(c("ompr", "ompr.roi", "ROI", "ROI.plugin.glpk"))
```

See the main [`README.md`](../README.md) for the example workflow.
